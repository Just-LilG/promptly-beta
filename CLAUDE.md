@AGENTS.md

Before any architectural work (database, auth, AI providers, deployment), read
@PROJECT_PLAN.md first. It records cross-session decisions and current live state that
aren't visible from the code alone.
